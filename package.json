{
  "name": "ai-story-teller",
  "version": "1.0.0",
  "description": "Illustrated AI Story Generator - MERN Stack Learner Edition",
  "scripts": {
    "install-all": "npm install --production=false --prefix client && npm install --production=false --prefix server",
    "build": "npm run build --prefix client",
    "start": "cross-env NODE_ENV=production node server/server.js",
    "dev-server": "npm run dev --prefix server",
    "dev-client": "npm run dev --prefix client"
  },
  "dependencies": {
    "cross-env": "^7.0.3"
  }
}
